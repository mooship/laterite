pre-commit:
  commands:
    format:
      glob: "*.{js,ts,astro,css,json,md,mdx}"
      run: pnpm exec prettier --write {staged_files}
      stage_fixed: true
    lint:
      glob: "*.{js,ts,astro}"
      run: pnpm exec eslint --fix {staged_files}
      stage_fixed: true
